# Changelog

The changelog lives at the repository root: [`CHANGELOG.md`](../CHANGELOG.md).

This file is kept as a thin pointer (rather than a second copy) so release
notes only need to be written once.
