{
  "name": "skill-manager",
  "version": "1.0.0",
  "description": "Interactive TUI for managing installed pskills across global and project scopes",
  "type": "module",
  "bin": {
    "skill-manager": "bin/skill-manager.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "typecheck": "bun run --bun tsc --noEmit",
    "dev": "bun run src/index.ts"
  },
  "dependencies": {
    "@opentui/core": "latest"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/luongnv89/skill-manager.git"
  },
  "devDependencies": {
    "bun-types": "^1.3.10"
  }
}
